# Full feature set. Version specifiers use compatible lower bounds (`>=`) so a
# clean install resolves to a current, mutually-compatible stack on modern
# Python (3.10+), while still guaranteeing a known-good minimum. For byte-exact
# reproducibility, generate a lock file with pip-tools (`pip-compile`).

# Web Scraping
requests>=2.31
beautifulsoup4>=4.12
selenium>=4.15
praw>=7.7
# snscrape upstream is largely unmaintained; used only by the Twitter scraper
snscrape>=0.7

# Data Processing
pandas>=2.1
numpy>=1.26
scipy>=1.11

# Machine Learning
scikit-learn>=1.3
statsmodels>=0.14
prophet>=1.1
xgboost>=2.0

# NLP
textblob>=0.17
spacy>=3.7
wordcloud>=1.9

# Visualization
plotly>=5.18
seaborn>=0.13
matplotlib>=3.8

# API service (Phase 3)
fastapi>=0.109
uvicorn[standard]>=0.27
httpx>=0.26

# Utilities
python-dotenv>=1.0
schedule>=1.2
pyyaml>=6.0
joblib>=1.3

# Development
jupyter>=1.0
ipykernel>=6.27
black>=23.12
pylint>=3.0
pytest>=7.4
