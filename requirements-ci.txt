# Lightweight dependency set for CI and for running the pipeline on synthetic
# data. Excludes heavy / network-only optional packages (prophet, selenium,
# snscrape, spacy) that are not required to run the test suite or main.py.
#
# For the full feature set (live scraping, Prophet forecasting, spaCy NLP)
# install requirements.txt instead.

# Data processing
pandas==2.1.3
numpy==1.26.2
scipy==1.11.4

# Machine learning
scikit-learn==1.3.2
statsmodels==0.14.0
xgboost==2.0.2

# NLP (light) + scraping core used by CDC scraper / sentiment utilities
textblob==0.17.1
requests==2.31.0
beautifulsoup4==4.12.2

# Visualization (imported by model_evaluator / eda_functions)
matplotlib==3.8.2
seaborn==0.13.0

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1
joblib==1.3.2

# Development / QA
pytest==7.4.3
black==23.12.0
pylint==3.0.3
